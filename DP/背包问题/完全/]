from typing import List
import math

class Solution:
    def numSquares(self, n: int) -> int:
        sqrt_nums = math.floor(math.sqrt(n))
        nums = [num**2 for num in range(1, sqrt_nums+1)]

if __name__ == '__main__':

